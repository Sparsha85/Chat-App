/* The component section has 5 components.
One is what controls the main chat window (Chat Feed),
the other takes in the message (Message Form),
another displays your messages (My messages),
another shows the third partie's messages (Their Message),
and one is the Login Form (LoginForm). 
*/
